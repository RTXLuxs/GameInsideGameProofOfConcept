using UnityEngine;

public enum ItemType
{
    Key, //Unlocks
    Tool, //Enables interactions
    Knowledge, //Items which contain information (codes, hints, etc.)
    Progression //Other required items which don't fall into categories above
}
